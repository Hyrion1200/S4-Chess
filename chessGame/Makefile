# Makefile
#
#
CC = gcc -fsanitize=address
CPPFLAGS =
CFLAGS = -Wall -Wextra -std=c99 -O0 -g3
LDFLAGS=
LDLIBS =

EXE = main piece validMoves board

all: ${EXE}

piece:
board:
validMoves:
main:

.PHONY: clean

clean:
	${RM} ${EXE}

#END
